{
  "packages": ["packages/*"],
  "version": "independent",
  "command": {
    "publish": {
      "ignoreChanges": ["*.md", "*.stories.*", "*.test.*"],
      "message": "chore(release): publish"
    }
  },
  "hoist": true,
  "stream": true
}
